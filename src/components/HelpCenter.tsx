import React from 'react';
import { Language } from '../types';
const HelpCenter: React.FC<{ lang: Language }> = () => (
  <div className="p-6 bg-white rounded-2xl shadow-xl">Help Center</div>
);
export default HelpCenter;
